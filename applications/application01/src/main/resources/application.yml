spring:
  application:
    name: application01
  output:
    ansi:
      enabled: always
  messages:
    basename: i18n/messages
    encoding: UTF-8
  shell:
    interactive:
      enabled: true

questionsFileName: questions.csv
countPasses: 3
